# 這個檔不被任何程式自動讀取，純粹是「要填哪些值」的範本。
# 複製成 .env（已被 .gitignore 忽略）後填值。

# ── CI 部署用（GitHub Actions 的 deploy-worker）──
# 在 Cloudflare 後台用「Edit Cloudflare Workers」範本建立 API Token，貼到這裡。
# 之後會被設成 repo 的 CLOUDFLARE_API_TOKEN secret。
CLOUDFLARE_API_TOKEN=paste-your-cloudflare-api-token-here
# 帳號 ID（已設進 repo secret，這裡列出只為完整）
CLOUDFLARE_ACCOUNT_ID=3a77813251d473c40d8873a59f6c0e80

# ── Worker 執行期 ──
# 注意：Cloudflare Workers 不讀 .env；本機開發請改用 .dev.vars（見 .dev.vars.example）。
# 正式環境用：wrangler secret put PRESENT_KEY
PRESENT_KEY=change-me
